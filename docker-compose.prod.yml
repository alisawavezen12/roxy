services:
  backend:
    image: roxy-backend-prod
    build:
      context: .
      dockerfile: Dockerfile
      target: backend-runtime
    init: true
    expose:
      - "8080"
    env_file:
      - .env
    environment:
      APP_ENV: production
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--output-document=/dev/null", "http://127.0.0.1:8080/ready"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s
    stop_grace_period: 10s
    networks:
      - production_internal
      - database_internal
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    image: roxy-frontend-prod
    build:
      context: .
      dockerfile: Dockerfile
      target: frontend-runtime
    init: true
    ports:
      - "127.0.0.1:8081:8080"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--output-document=/dev/null", "http://127.0.0.1:8080/health"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 5s
    stop_grace_period: 10s
    networks:
      - production_internal

  postgres:
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    stop_grace_period: 10s
    networks:
      - database_internal

  minio:
    profiles: ["local-infra"]

  minio-init:
    profiles: ["local-infra"]

networks:
  production_internal:
    internal: true
  database_internal:
    internal: true

volumes:
  postgres_data:
